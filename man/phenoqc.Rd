% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phenoqc.R
\name{phenoqc}
\alias{phenoqc}
\title{Run Complete Phenotypic Quality Control}
\usage{
phenoqc(
  data,
  trait_cols,
  row_col = "row",
  col_col = "col",
  gen_col = "genotype",
  rep_col = "rep",
  control_names = NULL,
  outlier_methods = c("iqr", "spatial"),
  iqr_threshold = 1.5,
  spatial_k = 8L,
  check_spatial_trend = TRUE,
  impute_missing = FALSE
)
}
\arguments{
\item{data}{A data frame of trial data.}

\item{trait_cols}{Character vector. Names of numeric trait columns to QC.}

\item{row_col}{Character. Row position column. Default \code{"row"}.}

\item{col_col}{Character. Column position column. Default \code{"col"}.}

\item{gen_col}{Character. Genotype column. Default \code{"genotype"}.}

\item{rep_col}{Character. Replicate column. Default \code{"rep"}.}

\item{control_names}{Character vector or NULL. Known check/control names.}

\item{outlier_methods}{Character vector. Outlier methods to apply.
Default \code{c("iqr", "spatial")}.}

\item{iqr_threshold}{Numeric. IQR multiplier for outlier detection.
Default 1.5.}

\item{spatial_k}{Integer. Neighbors for spatial outlier detection.
Default 8.}

\item{check_spatial_trend}{Logical. Whether to run spatial trend analysis.
Default TRUE.}

\item{impute_missing}{Logical. Whether to impute missing values using
spatial neighbors. Default FALSE.}
}
\value{
An S3 object of class \code{"phenoqc_result"} containing:
\describe{
\item{data}{The original data with all QC flag columns added.}
\item{validation}{Trial structure validation summary.}
\item{outliers}{Outlier summary per trait.}
\item{missing}{Missing data summary.}
\item{spatial}{Spatial trend diagnostics (if run).}
\item{cleaned_data}{Data with outliers set to NA.}
\item{summary}{One-line-per-trait overview.}
\item{params}{Parameters used for the QC run.}
}
}
\description{
The main entry point for phenoQC. Runs the full QC pipeline on a trial
data frame: structure validation, missing data summary, outlier detection,
and spatial trend diagnostics.
}
\examples{
\dontrun{
data(example_trial)
result <- phenoqc(
  example_trial,
  trait_cols = c("yield", "plant_height", "days_to_flower")
)
result
summary(result)
}

}
